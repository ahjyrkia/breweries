<h1>Listing styles</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @styles.each do |style| %>
      <tr>
        <td><%= link_to style.name, style %></td>
        <button type="button" class="btn btn-danger"><td><%= link_to 'Destroy', style, method: :delete, data: { confirm: 'Are you sure?' } %></td></button>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<button type="button" class="btn btn-info dropdown-toggle"><%= link_to 'New Style', new_style_path %></button>
